__pycache__/
*.pyc
.env
session/
store/*.db
logs/*.log
.venv/
venv/
.pytest_cache/

# Data mentah dari channel signal (berbayar/private) — jangan pernah publik
tests/fixtures/*.jsonl

# Data harga historis untuk backtest — besar, tidak perlu ikut commit
backtest/data/
.DS_Store
